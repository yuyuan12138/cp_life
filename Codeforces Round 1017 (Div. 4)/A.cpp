#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

void solve(){
    string a, b, c;
    cin >> a >> b >> c;
    cout << a[0] << b[0] << c[0] << endl;
}

int main(){
    int t;
    cin >> t;
    while(t--) solve();
}